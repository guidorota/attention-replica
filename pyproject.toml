[project]
name = "attention-replica"
version = "0.1.0"
description = "Attention Replica"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "datasets>=5.0.0",
    "dotenv>=0.9.9",
    "torch>=2.12.0",
]
